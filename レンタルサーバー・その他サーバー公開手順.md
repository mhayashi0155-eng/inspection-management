# レンタルサーバー・その他サーバーでの公開手順

このアプリケーションは、**HTML / CSS / JavaScript** だけで動作する「静的サイト」として作られています。
そのため、Vercel や Netlify だけでなく、**一般的なレンタルサーバー（エックスサーバー、さくらインターネット、ConoHaなど）** や、**社内サーバー** など、Webページが表示できる場所ならどこでも公開可能です。

## 公開の仕組み

このアプリの「データベース（データ保存場所）」は **Supabase** という外部のクラウドサービスを使っています。
そのため、サーバー側には「データベース機能」や「PHP/Rubyなどのプログラム動作環境」は不要です。単純に **ファイルを置くだけ** で動きます。

---

## 手順：レンタルサーバーへのアップロード (FTP)

一般的なレンタルサーバー（例: エックスサーバー、ロリポップなど）を使う場合の手順です。

### 1. 必要なもの

* レンタルサーバーの契約情報（FTPホスト名、ユーザー名、パスワード）
* FTPソフト（FileZilla, WinSCP など）
* デスクトップにある「点検表管理」フォルダの中身

### 2. アップロード手順

1. FTPソフトを使って、サーバーに接続します。
2. 公開用のフォルダ（`public_html` や `www` など、サーバーによって異なります）を開きます。
3. デスクトップの **「点検表管理」フォルダの中身すべて** を、その公開用フォルダの中にアップロード（ドラッグ＆ドロップ）します。
    * `index.html`
    * `script.js`
    * `style.css`
    * `assets` フォルダ
    * ...などすべて
    * ※ `.git` や `.vscode` などの隠しファイルはアップロードしなくて構いませんが、してしまっても動作に影響はありません。

### 3. アクセス確認

ブラウザで、そのサーバーのURL（例: `https://www.example.com/` や `https://www.example.com/tenken/`）にアクセスし、アプリが表示されるか確認してください。

---

## 【重要】LINE LIFF設定の更新

サーバーが変わると、アプリのURL（アドレス）が変わります。
LINEの中からアプリを開けるようにするため、**LINE Developers の設定変更が必須** です。

1. [LINE Developers コンソール](https://developers.line.biz/console/) にログインします。
2. 「LINEログイン」チャネル → **「LIFF」** タブを開きます。
3. 登録してあるLIFFアプリを選択します。
4. **「エンドポイントURL」** を、**新しいサーバーのURL** に書き換えて保存してください。
    * 変更前: `https://tenken-manage.vercel.app/` など
    * 変更後: `https://www.your-new-server.com/` (新しくアップロードした先のURL)

---

## その他の公開方法の例

### GitHub Pages を使う場合

もし GitHub をお使いであれば、リポジトリの **Settings > Pages** から、`Source` を「Master/Main」などに設定するだけで無料で公開できます。

### AWS S3 + CloudFront

AWS をお使いの場合は、S3 バケットにファイルをアップロードし、「静的ウェブサイトホスティング」を有効にするだけで公開できます。

---

## よくある質問

**Q: データベースの設定変更は必要ですか？**
A: **不要です。**
`script.js` の中に Supabase への接続情報（URLとKey）が書かれており、これはどのサーバーからアクセスしても同じデータベースに繋がるようになっています。
ですので、ファイルを移動させるだけで、これまで通り同じデータを見たり保存したりできます。
