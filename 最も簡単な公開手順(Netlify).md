# 最も簡単な公開方法：Netlify Drop（ドラッグ＆ドロップ）

`npx` コマンドが使えない場合、Node.js というソフトがインストールされていません。インストールし直すよりも、**「ブラウザにフォルダを放り込むだけ」**で公開できる **Netlify Drop** を使うのが一番早いです。

## 手順

1. **[Netlify Drop](https://app.netlify.com/drop)** にアクセスします。
    * ログインしなくても試せますが、後でURLを固定するために GitHub や Google でログインしておくことをお勧めします。

2. **フォルダを放り込む**
    * デスクトップにある **「点検表管理」** フォルダを、画面中央の点線エリアにそのままドラッグ＆ドロップします。

3. **公開完了！**
    * 数秒で `https://xxxx-xxxx.netlify.app` という URL が表示されます。
    * これがあなたのアプリの公開 URL です。

4. **LINE Developers の設定を更新する**
    * LINE Developers の LIFF 設定画面に行き、**「エンドポイントURL」** を、いま取得した Netlify の URL に書き換えて保存してください。

---

> [!TIP]
> **Node.js をインストールしたい場合**
> もし今後もコマンドを使って開発したい場合は、[Node.js 公式サイト](https://nodejs.org/ja/) から 「LTS (推奨版)」 をダウンロードしてインストールすれば、先ほどの `npx` コマンドが使えるようになります。
