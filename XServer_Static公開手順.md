# XServer Static での公開・更新手順

XServer Static と GitHub を連携させた後の、日々の更新手順を説明します。

## 1. 今後の更新手順（推奨：GitHub連携）

GitHub連携が完了している場合、**VS Code のターミナルからコマンドを3つ打つだけ** で、自動的に本番サーバー（XServer Static）も更新されます。
手動でファイルをアップロードする必要はありません。

### 手順

1. VS Code でファイルを編集・保存します。
2. 画面上部メニューの「ターミナル」→「新しいターミナル」を開きます。
3. 以下の3つのコマンドを順番に入力し、Enterキーを押してください。

**① 変更をステージング（準備）**

```bash
git add .
```

**② 変更をコミット（記録）**
※`" "` の中は「修正内容のメモ」です。日本語でもOKです。

```bash
git commit -m "修正内容のメモ"
```

**③ GitHubへ送信（プッシュ）**

```bash
git push
```

これだけで、GitHub にコードが保存され、自動的に XServer Static にも反映されます。
数分待ってから、Webサイトを確認してください。

---

## 2. 従来の方法（手動アップロード）

もし GitHub 連携がうまくいかない場合や、直接ファイルを置きたい場合は、以下の手順も引き続き利用可能です。

### ファイル管理を使う場合（少量更新）

1. 管理画面の青色のボタン **「ファイル管理」** をクリック。
2. `public_html` フォルダを開く。
3. ファイルをドラッグ＆ドロップで上書きアップロード。

### FTPソフトを使う場合（大量更新）

1. 管理画面の **「サーバー設定」** → 「FTP設定」で情報を確認。
2. FileZilla などのソフトで接続。
3. `/public_html/` フォルダ内にファイルをアップロード。

---

> [!TIP]
> **反映されない場合**
> ブラウザのキャッシュが残っている可能性があります。PCの場合は `Ctrl + F5`（強制リロード）を試してみてください。
